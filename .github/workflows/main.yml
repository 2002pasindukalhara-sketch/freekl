name: RDP with OBS Studio

on:
  # මෙය manual ධාවනය සඳහා වන trigger එකයි
  workflow_dispatch:

jobs:
  secure-rdp:
    # Windows runner එකක් භාවිතා කරයි
    runs-on: windows-latest
    # උපරිම ධාවන කාලය පැය 60 කි
    timeout-minutes: 3600

    steps:
      - name: Configure Core RDP Settings
        run: |
          Write-Host "Configuring RDP settings..."
          # Remote Desktop සක්‍රීය කිරීම
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' `
                             -Name "fDenyTSConnections" -Value 0 -Force
          # Network Level Authentication (NLA) අවශ්‍යතා disable කිරීම (Remote Desktop සඳහා)
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' `
                             -Name "UserAuthentication" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' `
                             -Name "SecurityLayer" -Value 0 -Force

          # Firewall rule එකක් එකතු කිරීම
          netsh advfirewall firewall delete rule name="RDP-Tailscale"
          netsh advfirewall firewall add rule name="RDP-Tailscale" `
            dir=in action=allow protocol=TCP localport=3389

          # RDP Service එක නැවත ආරම්භ කිරීම
          Restart-Service -Name TermService -Force

      - name: Create RDP User with Secure Password
        run: |
          Write-Host "Creating RDP user 'RDP'..."
          # ශක්තිමත් Password එකක් ජනනය කිරීම
          Add-Type -AssemblyName System.Security
          $charSet = @{
              Upper   = [char[]](65..90)
              Lower   = [char[]](97..122)
              Number  = [char[]](48..57)
              Special = ([char[]](33..47) + [char[]](58..64) +
                           [char[]](91..96) + [char[]](123..126))
          }
          $rawPassword = @()
          $rawPassword += $charSet.Upper | Get-Random -Count 4
          $rawPassword += $charSet.Lower | Get-Random -Count 4
          $rawPassword += $charSet.Number | Get-Random -Count 4
          $rawPassword += $charSet.Special | Get-Random -Count 4
          $password = -join ($rawPassword | Sort-Object { Get-Random })
          $securePass = ConvertTo-SecureString $password -AsPlainText -Force
          
          # New Local User නිර්මාණය කිරීම සහ Administrator Group වලට එකතු කිරීම
          New-LocalUser -Name "RDP" -Password $securePass -AccountNeverExpires
          Add-LocalGroupMember -Group "Administrators" -Member "RDP"
          Add-LocalGroupMember -Group "Remote Desktop Users" -Member "RDP"
          
          # RDP පිවිසුම් තොරතුරු environment variables වල save කිරීම
          echo "RDP_CREDS=User: RDP | Password: $password" >> $env:GITHUB_ENV
          
          if (-not (Get-LocalUser -Name "RDP")) {
              Write-Error "User creation failed"
              exit 1
          }
      
      - name: Install OBS Studio 🎬
        run: |
          # OBS Studio Installer එකේ Download link එක
          # නවතම අනුවාදය සඳහා මෙය යාවත්කාලීන කරන්න.
          $obsUrl = "https://cdn-fastly.obsproject.com/downloads/OBS-Studio-30.1.2-Full-Installer-x64.exe" 
          $installerPath = "$env:TEMP\OBS-Studio-Installer.exe"
          
          Write-Host "Downloading OBS Studio..."
          Invoke-WebRequest -Uri $obsUrl -OutFile $installerPath -TimeoutSec 300
          
          Write-Host "Installing OBS Studio silently..."
          # /S switch එක භාවිතයෙන් Silent Installation සිදු කිරීම.
          Start-Process -FilePath $installerPath -ArgumentList "/S" -Wait
          
          Write-Host "OBS Studio installation complete."
          Remove-Item $installerPath -Force
      
      - name: Install Tailscale
        run: |
          Write-Host "Installing Tailscale..."
          $tsUrl = "https://pkgs.tailscale.com/stable/tailscale-setup-1.82.0-amd64.msi"
          $installerPath = "$env:TEMP\tailscale.msi"
          
          Invoke-WebRequest -Uri $tsUrl -OutFile $installerPath
          Start-Process msiexec.exe -ArgumentList "/i", "`"$installerPath`"", "/quiet", "/norestart" -Wait
          Remove-Item $installerPath -Force

      - name: Establish Tailscale Connection
        run: |
          Write-Host "Establishing Tailscale connection..."
          # Tailscale සම්බන්ධතාවය ආරම්භ කිරීම (ඔබේ repo secrets හි TAILSCALE_AUTH_KEY තිබිය යුතුය)
          & "$env:ProgramFiles\Tailscale\tailscale.exe" up --authkey=${{ secrets.TAILSCALE_AUTH_KEY }} --hostname=gh-runner-$env:GITHUB_RUN_ID
          
          # Tailscale IP එකක් ලැබෙන තෙක් රැඳී සිටීම
          $tsIP = $null
          $retries = 0
          while (-not $tsIP -and $retries -lt 10) {
              $tsIP = & "$env:ProgramFiles\Tailscale\tailscale.exe" ip -4
              Start-Sleep -Seconds 5
              $retries++
          }
          
          if (-not $tsIP) {
              Write-Error "Tailscale IP not assigned. Exiting."
              exit 1
          }
          echo "TAILSCALE_IP=$tsIP" >> $env:GITHUB_ENV
      
      - name: Verify RDP Accessibility
        run: |
          Write-Host "Verifying RDP accessibility on IP: $env:TAILSCALE_IP"
          # RDP port එකේ (3389) සම්බන්ධතාවය පරීක්ෂා කිරීම
          $testResult = Test-NetConnection -ComputerName $env:TAILSCALE_IP -Port 3389
          if (-not $testResult.TcpTestSucceeded) {
              Write-Error "TCP connection to RDP port 3389 failed"
              exit 1
          }
          Write-Host "TCP connectivity successful!"

      - name: Maintain Connection and Display Info
        run: |
          Write-Host "`n===================================="
          Write-Host "       RDP ACCESS INFORMATION       "
          Write-Host "===================================="
          Write-Host "Address (Tailscale IP): $env:TAILSCALE_IP"
          Write-Host "Username: RDP"
          Write-Host "Password: $(echo $env:RDP_CREDS)"
          Write-Host "====================================`n"
          
          # Workflow එක ක්‍රියාකාරීව තබා ගැනීම (Stop වන තුරු)
          Write-Host "Runner is active. Connect via RDP using the info above."
          while ($true) {
              Start-Sleep -Seconds 300 # සෑම විනාඩි 5කට වරක් ලොග් පණිවිඩයක් දමයි
          }
